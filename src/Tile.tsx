import React from 'react';

interface TileProps {
    value: number;
}
const Tile: React.FC<TileProps> = ({value}) => {

    // const isEmpty = value === 0;

    return (
        <div>

        </div>
    );
};

export default Tile
